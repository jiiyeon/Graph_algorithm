#include <iostream>
#include <vector>

int main()
{
    int N, M;
    std::cin >> N >> M;

    // 1부터 N까지의 배열 만들기
    int num_arr[N];
    int i=0;
    while (i<N)
    {
        num_arr[i] = i;
        i++;
    }

    // M 개씩 꺼내어 경우의 수 만들기
    

    return 0;
}
